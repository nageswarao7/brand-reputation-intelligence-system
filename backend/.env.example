# OpenAI API Configuration
# Replace this with your actual OpenAI API key (starting with sk-proj-...)
# If this is left blank or not configured, the pipeline will fallback to a local heuristic classifier.
OPENAI_API_KEY=your_openai_api_key_here
